/**
 * @type {import('next').NextConfig}
 */

const nextConfig = {
  images: {
    unoptimized: true,
    path: "",
  },
  assetPrefix: "",
  output: "export",
  basePath: "",
};

module.exports = nextConfig;
